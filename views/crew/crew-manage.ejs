<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SPORTMATCH — 크루 관리</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=DM+Mono:wght@400;500;700&family=Bebas+Neue&display=swap" rel="stylesheet">
<link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
<%- include('../partials/navbar') %>

<div class="main">
<%- include('../partials/head', { title: '🔍 크루 관리', pageId: 'page-crew-manage', active: '' }) %>

  <div class="page-body">
  <div class="detail-wrap">

  <!-- 크루 헤더 요약 -->
  <div class="manage-group" style="margin-bottom:8px;">
    <div class="manage-header" style="border-bottom:none;">
      <img src="<%= crew.profileImage %>" class="manage-sport-icon-emoji">
      <div class="manage-header-info">
        <div class="manage-title"><%= crew.title %></div>
        <div class="manage-sub"><%= crew.address.state %> · <%= crew.address.city %></div>
      </div>
    </div>
  </div>

  <!-- 탭 네비게이션 -->
  <div class="manage-tab-bar">
    <button class="manage-tab active" onclick="switchTab('info', this)">
      <span>📋</span> 크루 정보
    </button>
    <button class="manage-tab" onclick="switchTab('schedule', this)">
      <span>📅</span> 스케줄
    </button>
    <button class="manage-tab" onclick="switchTab('members', this)">
      <span>👥</span> 멤버 관리
      <span class="tab-badge">2</span>
    </button>
  </div>

  <!-- ═══ 탭 1: 크루 정보 ═══ -->
  <div class="manage-panel active" id="panel-info">

    <!-- 프로필 이미지 -->
    <div class="manage-section">
      <div class="manage-section-header">
        <div class="manage-section-title">🖼️ 프로필 이미지</div>
      </div>
      <div class="manage-section-body" style="padding: 12px 20px;">
        <div class="img-upload-area">
          <div class="avatar-section" style="border:none"> 
            <div class="avatar-preview-wrap">
              <div class="avatar-preview">
                  <img id="profileImage" src="/images/reg-crew/profile/default-profile-image.jpg" alt="profile"/>
              </div>
              <button type="button" class="avatar-remove-btn" id="profile-reset">x</button>
              <label class="avatar-edit-label" for="profile-input">+</label>
              <input type="file" id="profile-input" name="uploadFile" accept="image/*" style="display:none">
              <input type="hidden" id="removeImage" name="removeImage" value="false">
            </div>
          </div>
          <div class="img-upload-info">
            <div class="img-upload-title">크루 프로필 이미지</div>
            <div class="img-upload-sub">JPG, PNG · 최대 5MB<br>정사각형 이미지를 권장합니다</div>
          </div>
        </div>
      </div>
    </div>

    <!-- 기본 정보 -->
    <div class="manage-section">
      <div class="manage-section-header">
        <div class="manage-section-title">📝 크루 기본 정보</div>
      </div>
      <div class="manage-section-body">
        <div class="field">
          <label class="label">크루 이름</label>
          <input type="text" class="manage-input" id="crewTitle" value="<%= crew.title %>" placeholder="크루 이름을 입력하세요">
        </div>
        <div class="field">
          <label class="label">크루 소개글</label>
          <textarea class="manage-input" id="crewIntro" style="min-height:110px;"><%= crew.intro %></textarea>
        </div>
      </div>
    </div>

    <!-- 가입 조건 -->
    <div class="manage-section">
      <div class="manage-section-header">
        <div class="manage-section-title">📋 가입 조건 수정</div>
      </div>
      <div class="manage-section-body">
        <div class="input-row" style="margin-bottom:12px; grid-template-columns: repeat(3, 1fr);">
          <div class="field" style="margin-bottom:0">
            <label class="label">최대 인원 (2 ~ 50)</label>
            <input type="number" class="manage-input" id="capacity" value="<%= crew.member.capacity %>" min="2" max="50">
          </div>
          <div class="field" style="margin-bottom:0">
            <label class="label">참가비 (원)</label>
            <input type="number" class="manage-input" id="fee" value="<%= crew.fee %>" min="0">
          </div>
          <div class="field" style="margin-bottom:0">
            <label class="label">주 모임 장소</label>
            <input type="text" class="manage-input" id="detail" value="<%= crew.address.detail %>">
          </div>
        </div>
        <div class="input-row" style="margin-bottom:12px; grid-template-columns: repeat(3, 1fr);">
          <div class="field" style="margin-bottom:0">
            <label class="label">종목</label>
            <select class="manage-input" id="period">
              <option value="soccer" <%= crew.sport === 'soccer' ? 'selected' : '' %>>축구</option>
              <option value="baseball" <%= crew.sport === 'baseball' ? 'selected' : '' %>>야구</option>
              <option value="basketball" <%= crew.sport === 'basketball' ? 'selected' : '' %>>농구</option>
              <option value="bowling" <%= crew.sport === 'bowling' ? 'selected' : '' %>>볼링</option>
              <option value="tennis" <%= crew.sport === 'tennis' ? 'selected' : '' %>>테니스</option>
              <option value="badminton" <%= crew.sport === 'badminton' ? 'selected' : '' %>>배드민턴</option>
              <option value="tabletennis" <%= crew.sport === 'tabletennis' ? 'selected' : '' %>>탁구</option>
            </select>
          </div>
          <div class="field" style="margin-bottom:0">
            <label class="label">주기</label>
            <select class="manage-input" id="period">
              <option value="week" <%= crew.period === 'week' ? 'selected' : '' %>>매주</option>
              <option value="2week" <%= crew.period === '2week' ? 'selected' : '' %>>격주</option>
              <option value="month" <%= crew.period === 'month' ? 'selected' : '' %>>매달</option>
            </select>
          </div>
          <div class="field" style="margin-bottom:0">
            <label class="label">실력 수준</label>
            <select class="manage-input" id="level">
              <option value="none" <%= crew.level === 'none' ? 'selected' : '' %>>무관</option>
              <option value="low" <%= crew.level === 'low' ? 'selected' : '' %>>초보</option>
              <option value="middle" <%= crew.level === 'mid' ? 'selected' : '' %>>중급</option>
              <option value="high" <%= crew.level === 'high' ? 'selected' : '' %>>고급</option>
            </select>
          </div>
        </div>
        <div class="field" style="margin-bottom:0">
          <label class="label">활동 요일</label>
          <div class="chip-group" style="margin-top:6px;">
            <button class="chip <%= crew.day.includes('mon') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="월">월</button>
            <button class="chip <%= crew.day.includes('tue') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="화">화</button>
            <button class="chip <%= crew.day.includes('wed') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="수">수</button>
            <button class="chip <%= crew.day.includes('thu') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="목">목</button>
            <button class="chip <%= crew.day.includes('fri') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="금">금</button>
            <button class="chip <%= crew.day.includes('sat') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="토">토</button>
            <button class="chip <%= crew.day.includes('sun') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="일">일</button>
          </div>
        </div>
        <br>
        <div class="field" style="margin-bottom:0">
          <label class="label">연령대</label>
          <div class="chip-group" style="margin-top:6px;">
            <button class="chip <%= crew.ageRange.includes('10s') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="10s">10대</button>
            <button class="chip <%= crew.ageRange.includes('20s') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="20s">20대</button>
            <button class="chip <%= crew.ageRange.includes('30s') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="30s">30대</button>
            <button class="chip <%= crew.ageRange.includes('40s') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="40s">40대</button>
            <button class="chip <%= crew.ageRange.includes('50s') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="50s">50대</button>
            <button class="chip <%= crew.ageRange.includes('60+') ? 'on' : '' %>" onclick="toggleChip(this)" data-val="60+">60대 이상</button>
          </div>
        </div>
        <br>
        <div class="field" style="margin-bottom:0">
          <label class="label">가입 승인 방식</label>
          <div class="chip-group" style="margin-top:6px;">
            <button class="chip <%= crew.isAutoAccept ? 'on' : '' %>" onclick="selectSingle(this, 'accept-group')" data-group="accept-group" data-val="auto">자동 승인</button>
            <button class="chip <%= !crew.isAutoAccept ? 'on' : '' %>" onclick="selectSingle(this, 'accept-group')" data-group="accept-group" data-val="manual">수동 승인</button>
          </div>
        </div>
      </div>
    </div>

    <!-- 저장 -->
    <div class="save-bar">
      <button class="btn btn-outline" onclick="showToast('⚠️ 변경사항이 취소되었습니다')">취소</button>
      <button class="btn btn-primary" onclick="showToast('✅ 크루 정보가 저장되었습니다')">💾 변경사항 저장</button>
    </div>
  </div>

  <!-- ═══ 탭 2: 스케줄 ═══ -->
  <div class="manage-panel" id="panel-schedule">

    <div class="manage-section">
      <div class="manage-section-header">
        <div class="manage-section-title">
          <span class="section-icon">📅</span>
          정기 스케줄 시간표
        </div>
        <button class="btn btn-primary btn-sm" onclick="toggleAddForm()">+ 일정 추가</button>
      </div>
      <div class="manage-section-body">
        <p style="font-size:12.5px;color:var(--text-3);margin-bottom:14px;">
          셀을 클릭하거나 드래그해서 활동 시간을 선택하고 저장하세요.
        </p>

        <!-- 시간표 -->
        <div class="timetable-wrap">
          <div class="timetable" id="timetable">
            <div class="tt-header-cell" style="background:var(--bg)"></div>
            <div class="tt-header-cell day-sun">일</div>
            <div class="tt-header-cell">월</div>
            <div class="tt-header-cell">화</div>
            <div class="tt-header-cell">수</div>
            <div class="tt-header-cell">목</div>
            <div class="tt-header-cell">금</div>
            <div class="tt-header-cell day-sat">토</div>
          </div>
        </div>

        <div class="timetable-legend">
          <div class="legend-item">
            <div class="legend-dot" style="background:rgba(26,92,255,.4);border:1px solid var(--border)"></div>
            <span>등록된 일정</span>
          </div>
          <div class="legend-item">
            <div class="legend-dot" style="background:rgba(200,255,0,.5);border:1px solid var(--border)"></div>
            <span>선택 중</span>
          </div>
        </div>
      </div>
    </div>

    <!-- 등록된 일정 목록 -->
    <div class="manage-section">
      <div class="manage-section-header">
        <div class="manage-section-title">
          <span class="section-icon">📌</span>
          등록된 정기 일정
        </div>
        <span class="tag tag-outline" id="scheduleCount">2개</span>
      </div>
      <div class="schedule-event-list" id="scheduleList" style="padding:16px;">

        <!-- 일정 1 -->
        <div class="schedule-event-item" id="sch-0">
          <div class="event-day-badge sat">토</div>
          <div class="event-info">
            <div class="event-title">주말 정기 농구</div>
            <div class="event-meta">⏰ 10:00 – 12:00 &nbsp;·&nbsp; 📍 마포 한강 농구장</div>
          </div>
          <div class="event-actions">
            <button class="event-edit-btn" onclick="toggleEditForm(0)">수정</button>
            <button class="event-del-btn" onclick="deleteSchedule(0)">삭제</button>
          </div>
        </div>
        <!-- 수정 인라인 폼 (일정 1) -->
        <div class="inline-edit-form" id="edit-form-0">
          <div style="font-size:12.5px;font-weight:700;color:var(--primary);margin-bottom:12px;">✏️ 일정 수정</div>
          <div class="inline-edit-grid">
            <div class="field full" style="margin-bottom:0">
              <label class="label">일정 이름</label>
              <input type="text" class="manage-input" value="주말 정기 농구">
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">시작 시간</label>
              <input type="time" class="manage-input" value="10:00">
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">종료 시간</label>
              <input type="time" class="manage-input" value="12:00">
            </div>
            <div class="field full" style="margin-bottom:0">
              <label class="label">장소</label>
              <input type="text" class="manage-input" value="마포 한강 농구장">
            </div>
          </div>
          <div class="inline-edit-actions">
            <button class="btn btn-ghost btn-sm" onclick="toggleEditForm(0)">취소</button>
            <button class="btn btn-primary btn-sm" onclick="saveEdit(0)">저장</button>
          </div>
        </div>

        <!-- 일정 2 -->
        <div class="schedule-event-item" id="sch-1">
          <div class="event-day-badge sun">일</div>
          <div class="event-info">
            <div class="event-title">일요일 자유 게임</div>
            <div class="event-meta">⏰ 14:00 – 16:00 &nbsp;·&nbsp; 📍 마포 한강 농구장</div>
          </div>
          <div class="event-actions">
            <button class="event-edit-btn" onclick="toggleEditForm(1)">수정</button>
            <button class="event-del-btn" onclick="deleteSchedule(1)">삭제</button>
          </div>
        </div>
        <!-- 수정 인라인 폼 (일정 2) -->
        <div class="inline-edit-form" id="edit-form-1">
          <div style="font-size:12.5px;font-weight:700;color:var(--primary);margin-bottom:12px;">✏️ 일정 수정</div>
          <div class="inline-edit-grid">
            <div class="field full" style="margin-bottom:0">
              <label class="label">일정 이름</label>
              <input type="text" class="manage-input" value="일요일 자유 게임">
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">시작 시간</label>
              <input type="time" class="manage-input" value="14:00">
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">종료 시간</label>
              <input type="time" class="manage-input" value="16:00">
            </div>
            <div class="field full" style="margin-bottom:0">
              <label class="label">장소</label>
              <input type="text" class="manage-input" value="마포 한강 농구장">
            </div>
          </div>
          <div class="inline-edit-actions">
            <button class="btn btn-ghost btn-sm" onclick="toggleEditForm(1)">취소</button>
            <button class="btn btn-primary btn-sm" onclick="saveEdit(1)">저장</button>
          </div>
        </div>

        <!-- 일정 추가 폼 -->
        <div class="add-schedule-form" id="addScheduleForm">
          <div class="add-form-title">📅 새 일정 추가</div>
          <div class="add-form-grid">
            <div class="field full" style="margin-bottom:0">
              <label class="label">일정 이름</label>
              <input type="text" class="manage-input" id="new-title" placeholder="예: 주말 정기 농구">
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">요일</label>
              <select class="manage-input" id="new-day">
                <option value="일">일요일</option>
                <option value="월">월요일</option>
                <option value="화">화요일</option>
                <option value="수">수요일</option>
                <option value="목">목요일</option>
                <option value="금">금요일</option>
                <option value="토">토요일</option>
              </select>
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">장소</label>
              <input type="text" class="manage-input" id="new-location" placeholder="예: OO 체육관">
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">시작 시간</label>
              <input type="time" class="manage-input" id="new-start" value="10:00">
            </div>
            <div class="field" style="margin-bottom:0">
              <label class="label">종료 시간</label>
              <input type="time" class="manage-input" id="new-end" value="12:00">
            </div>
          </div>
          <div class="add-form-actions">
            <button class="btn btn-ghost btn-sm" onclick="toggleAddForm()">취소</button>
            <button class="btn btn-primary btn-sm" onclick="addSchedule()">+ 추가</button>
          </div>
        </div>

      </div>
    </div>

    <!-- 저장 -->
    <div class="save-bar">
      <button class="btn btn-primary" onclick="showToast('✅ 스케줄이 저장되었습니다')">💾 스케줄 저장</button>
    </div>
  </div>

  <!-- ═══ 탭 3: 멤버 관리 ═══ -->
  <div class="manage-panel" id="panel-members">

    <!-- 멤버 통계 -->
    <div class="member-stat-row">
      <div class="member-stat-card">
        <span class="member-stat-val">8</span>
        <span class="member-stat-label">현재 멤버</span>
      </div>
      <div class="member-stat-card">
        <span class="member-stat-val">20</span>
        <span class="member-stat-label">최대 인원</span>
      </div>
      <div class="member-stat-card" style="border-color:#fecaca;">
        <span class="member-stat-val" style="color:#ef4444;">2</span>
        <span class="member-stat-label">승인 대기</span>
      </div>
    </div>

    <!-- 가입 신청 대기 -->
    <div class="manage-section">
      <div class="manage-section-header">
        <div class="manage-section-title">
          <span class="section-icon">⏳</span>
          가입 신청 대기
          <span class="tag tag-red">2</span>
        </div>
      </div>

      <div class="member-request-item" id="req-u001">
        <div class="member-av" style="background:linear-gradient(135deg,#7c3aed,#a855f7)">김</div>
        <div class="member-info">
          <div class="member-name">김도현</div>
          <div class="member-meta">20대 · Lv.2 &nbsp;·&nbsp; "농구 좋아하는 직장인입니다!"</div>
        </div>
        <div class="member-action-btns">
          <button class="btn btn-outline btn-sm">👤 프로필</button>
          <button class="approve-btn" onclick="handleRequest('req-u001', 'approve')">✓ 승인</button>
          <button class="reject-btn" onclick="handleRequest('req-u001', 'reject')">✗ 거절</button>
        </div>
      </div>

      <div class="member-request-item" id="req-u002">
        <div class="member-av" style="background:linear-gradient(135deg,#ea580c,#f97316)">이</div>
        <div class="member-info">
          <div class="member-name">이수진</div>
          <div class="member-meta">20대 · Lv.1 &nbsp;·&nbsp; "처음 시작하는 농구 초보입니다 😊"</div>
        </div>
        <div class="member-action-btns">
          <button class="btn btn-outline btn-sm">👤 프로필</button>
          <button class="approve-btn" onclick="handleRequest('req-u002', 'approve')">✓ 승인</button>
          <button class="reject-btn" onclick="handleRequest('req-u002', 'reject')">✗ 거절</button>
        </div>
      </div>
    </div>

    <!-- 멤버 목록 -->
    <div class="manage-section">
      <div class="manage-section-header">
        <div class="manage-section-title">
          <span class="section-icon">👥</span>
          멤버 목록
        </div>
        <span class="tag tag-outline">8명</span>
      </div>

      <div class="member-list-item" id="member-host">
        <div class="member-av" style="background:linear-gradient(135deg,#1A5CFF,#00C8D4)">박</div>
        <div class="member-info">
          <div class="member-name">박준혁</div>
          <div class="member-meta">30대 · 가입 2024.03.01</div>
        </div>
        <span class="member-role-tag host">👑 크루장</span>
      </div>

      <div class="member-list-item" id="member-m001">
        <div class="member-av" style="background:linear-gradient(135deg,#16a34a,#22c55e)">최</div>
        <div class="member-info">
          <div class="member-name">최민준</div>
          <div class="member-meta">20대 · 가입 2024.04.10</div>
        </div>
        <span class="member-role-tag member">멤버</span>
        <button class="kick-btn" onclick="kickMember('member-m001', '최민준')">강퇴</button>
      </div>

      <div class="member-list-item" id="member-m002">
        <div class="member-av" style="background:linear-gradient(135deg,#b45309,#d97706)">정</div>
        <div class="member-info">
          <div class="member-name">정유진</div>
          <div class="member-meta">20대 · 가입 2024.04.15</div>
        </div>
        <span class="member-role-tag member">멤버</span>
        <button class="kick-btn" onclick="kickMember('member-m002', '정유진')">강퇴</button>
      </div>

      <div class="member-list-item" id="member-m003">
        <div class="member-av" style="background:linear-gradient(135deg,#0891b2,#06b6d4)">강</div>
        <div class="member-info">
          <div class="member-name">강태양</div>
          <div class="member-meta">30대 · 가입 2024.05.02</div>
        </div>
        <span class="member-role-tag member">멤버</span>
        <button class="kick-btn" onclick="kickMember('member-m003', '강태양')">강퇴</button>
      </div>

      <div class="member-list-item" id="member-m004">
        <div class="member-av" style="background:linear-gradient(135deg,#9333ea,#a855f7)">윤</div>
        <div class="member-info">
          <div class="member-name">윤서현</div>
          <div class="member-meta">20대 · 가입 2024.05.18</div>
        </div>
        <span class="member-role-tag member">멤버</span>
        <button class="kick-btn" onclick="kickMember('member-m004', '윤서현')">강퇴</button>
      </div>

      <div class="member-list-item" id="member-m005">
        <div class="member-av" style="background:linear-gradient(135deg,#dc2626,#ef4444)">한</div>
        <div class="member-info">
          <div class="member-name">한지수</div>
          <div class="member-meta">20대 · 가입 2024.06.03</div>
        </div>
        <span class="member-role-tag member">멤버</span>
        <button class="kick-btn" onclick="kickMember('member-m005', '한지수')">강퇴</button>
      </div>

      <div class="member-list-item" id="member-m006">
        <div class="member-av" style="background:linear-gradient(135deg,#065f46,#059669)">오</div>
        <div class="member-info">
          <div class="member-name">오재원</div>
          <div class="member-meta">30대 · 가입 2024.06.20</div>
        </div>
        <span class="member-role-tag member">멤버</span>
        <button class="kick-btn" onclick="kickMember('member-m006', '오재원')">강퇴</button>
      </div>

      <div class="member-list-item" id="member-m007">
        <div class="member-av" style="background:linear-gradient(135deg,#1e40af,#3b82f6)">신</div>
        <div class="member-info">
          <div class="member-name">신아름</div>
          <div class="member-meta">20대 · 가입 2024.07.05</div>
        </div>
        <span class="member-role-tag member">멤버</span>
        <button class="kick-btn" onclick="kickMember('member-m007', '신아름')">강퇴</button>
      </div>

    </div>
  </div>

  

  </div><!-- /detail-wrap -->
  </div><!-- /page-body -->
</div><!-- /main -->

<!-- 토스트 -->
<div class="manage-toast" id="manageToast">
  <span id="toastMsg">저장되었습니다</span>
</div>

<script>
// ── 탭 전환 ──
function switchTab(name, btn) {
  document.querySelectorAll('.manage-tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.manage-panel').forEach(p => p.classList.remove('active'));
  btn.classList.add('active');
  document.getElementById('panel-' + name).classList.add('active');
}

// ── 칩 토글 ──
function toggleChip(el) { el.classList.toggle('on'); }
function selectSingle(el, group) {
  document.querySelectorAll(`[data-group="${group}"]`).forEach(c => c.classList.remove('on'));
  el.classList.add('on');
}

// ── 이미지 미리보기 ──
function previewImg(input) {
  if (input.files && input.files[0]) {
    const reader = new FileReader();
    reader.onload = e => { document.getElementById('imgPreview').src = e.target.result; };
    reader.readAsDataURL(input.files[0]);
  }
}
function resetImg() {
  document.getElementById('imgPreview').src = 'https://placehold.co/88x88/1A5CFF/ffffff?text=🏀';
  document.getElementById('imgInput').value = '';
}

// ── 일정 추가 폼 토글 ──
function toggleAddForm() {
  document.getElementById('addScheduleForm').classList.toggle('show');
}

// ── 일정 추가 ──
function addSchedule() {
  const title    = document.getElementById('new-title').value.trim() || '정기 활동';
  const day      = document.getElementById('new-day').value;
  const start    = document.getElementById('new-start').value;
  const end      = document.getElementById('new-end').value;
  const location = document.getElementById('new-location').value.trim() || '미정';

  if (!start || !end || start >= end) {
    showToast('⚠️ 올바른 시간을 입력해주세요'); return;
  }

  const list = document.getElementById('scheduleList');
  const idx  = list.querySelectorAll('.schedule-event-item').length;
  const isSun = day === '일', isSat = day === '토';
  const badgeClass = isSun ? 'sun' : isSat ? 'sat' : '';

  const item = document.createElement('div');
  item.className = 'schedule-event-item';
  item.id = 'sch-' + idx;
  item.innerHTML = `
    <div class="event-day-badge ${badgeClass}">${day}</div>
    <div class="event-info">
      <div class="event-title">${title}</div>
      <div class="event-meta">⏰ ${start} – ${end} &nbsp;·&nbsp; 📍 ${location}</div>
    </div>
    <div class="event-actions">
      <button class="event-edit-btn" onclick="toggleEditForm(${idx})">수정</button>
      <button class="event-del-btn" onclick="deleteSchedule(${idx})">삭제</button>
    </div>`;

  const editForm = document.createElement('div');
  editForm.className = 'inline-edit-form';
  editForm.id = 'edit-form-' + idx;
  editForm.innerHTML = `
    <div style="font-size:12.5px;font-weight:700;color:var(--primary);margin-bottom:12px;">✏️ 일정 수정</div>
    <div class="inline-edit-grid">
      <div class="field full" style="margin-bottom:0">
        <label class="label">일정 이름</label>
        <input type="text" class="manage-input" value="${title}">
      </div>
      <div class="field" style="margin-bottom:0">
        <label class="label">시작 시간</label>
        <input type="time" class="manage-input" value="${start}">
      </div>
      <div class="field" style="margin-bottom:0">
        <label class="label">종료 시간</label>
        <input type="time" class="manage-input" value="${end}">
      </div>
      <div class="field full" style="margin-bottom:0">
        <label class="label">장소</label>
        <input type="text" class="manage-input" value="${location}">
      </div>
    </div>
    <div class="inline-edit-actions">
      <button class="btn btn-ghost btn-sm" onclick="toggleEditForm(${idx})">취소</button>
      <button class="btn btn-primary btn-sm" onclick="saveEdit(${idx})">저장</button>
    </div>`;

  // 추가 폼 앞에 삽입
  const addForm = document.getElementById('addScheduleForm');
  list.insertBefore(editForm, addForm);
  list.insertBefore(item, editForm);

  // 카운트 업데이트
  const countEl = document.getElementById('scheduleCount');
  countEl.textContent = (parseInt(countEl.textContent) + 1) + '개';

  // 폼 초기화 및 닫기
  document.getElementById('new-title').value = '';
  document.getElementById('new-location').value = '';
  toggleAddForm();
  showToast('✅ 일정이 추가되었습니다');
}

// ── 수정 폼 토글 ──
function toggleEditForm(idx) {
  const form = document.getElementById('edit-form-' + idx);
  if (form) form.classList.toggle('show');
}

// ── 일정 저장 ──
function saveEdit(idx) {
  const form = document.getElementById('edit-form-' + idx);
  const inputs = form.querySelectorAll('input');
  const newTitle    = inputs[0].value.trim() || '정기 활동';
  const newStart    = inputs[1].value;
  const newEnd      = inputs[2].value;
  const newLocation = inputs[3].value.trim() || '미정';

  if (newStart >= newEnd) { showToast('⚠️ 올바른 시간을 입력해주세요'); return; }

  const item = document.getElementById('sch-' + idx);
  item.querySelector('.event-title').textContent = newTitle;
  item.querySelector('.event-meta').innerHTML = `⏰ ${newStart} – ${newEnd} &nbsp;·&nbsp; 📍 ${newLocation}`;

  form.classList.remove('show');
  showToast('✅ 일정이 수정되었습니다');
}

// ── 일정 삭제 ──
function deleteSchedule(idx) {
  if (!confirm('이 일정을 삭제할까요?')) return;
  const item = document.getElementById('sch-' + idx);
  const form = document.getElementById('edit-form-' + idx);
  if (item) { item.style.opacity='0'; item.style.transition='opacity .3s'; setTimeout(() => item.remove(), 300); }
  if (form) setTimeout(() => form.remove(), 300);

  const countEl = document.getElementById('scheduleCount');
  const cur = parseInt(countEl.textContent);
  if (cur > 0) countEl.textContent = (cur - 1) + '개';
  showToast('🗑️ 일정이 삭제되었습니다');
}

// ── 가입 신청 처리 ──
function handleRequest(elId, action) {
  const el = document.getElementById(elId);
  if (el) {
    el.style.transition = 'opacity .3s, transform .3s';
    el.style.opacity = '0';
    el.style.transform = 'translateX(20px)';
    setTimeout(() => el.remove(), 300);
  }
  // 대기 카운트 업데이트
  const statVal = document.querySelector('.member-stat-val[style*="color:#ef4444"]');
  if (statVal) {
    const cur = parseInt(statVal.textContent);
    if (cur > 0) statVal.textContent = cur - 1;
  }
  showToast(action === 'approve' ? '✅ 승인되었습니다' : '❌ 거절되었습니다');
}

// ── 멤버 강퇴 ──
function kickMember(elId, name) {
  if (!confirm(`'${name}' 멤버를 정말 강퇴하시겠습니까?`)) return;
  const el = document.getElementById(elId);
  if (el) {
    el.style.transition = 'opacity .3s';
    el.style.opacity = '0';
    setTimeout(() => el.remove(), 300);
  }
  showToast('🚫 멤버가 강퇴되었습니다');
}

// ── 시간표 ──
const START_HOUR = 6;
const END_HOUR   = 23;
const DAYS = ['일','월','화','수','목','금','토'];

// 하드코딩된 기존 일정 (토 10~12, 일 14~16)
const savedCells = new Set([
  '6-10','6-11',  // 토 10:00~12:00
  '0-14','0-15'   // 일 14:00~16:00
]);

let isDragging = false;
let dragMode = null;

function buildTimetable() {
  const tt = document.getElementById('timetable');
  for (let h = START_HOUR; h < END_HOUR; h++) {
    const timeCell = document.createElement('div');
    timeCell.className = 'tt-time-cell';
    timeCell.textContent = `${String(h).padStart(2,'0')}:00`;
    tt.appendChild(timeCell);

    for (let d = 0; d < 7; d++) {
      const cell = document.createElement('div');
      cell.className = 'tt-cell';
      const key = `${d}-${h}`;
      if (savedCells.has(key)) cell.classList.add('has-event');
      cell.dataset.day = d;
      cell.dataset.hour = h;

      cell.addEventListener('mousedown', e => {
        e.preventDefault();
        isDragging = true;
        dragMode = cell.classList.contains('selected') ? 'remove' : 'add';
        toggleCell(cell);
      });
      cell.addEventListener('mouseover', () => {
        if (isDragging) toggleCell(cell);
      });
      tt.appendChild(cell);
    }
  }
  document.addEventListener('mouseup', () => { isDragging = false; });
}

function toggleCell(cell) {
  if (dragMode === 'add') cell.classList.add('selected');
  else cell.classList.remove('selected');
}

buildTimetable();

// ── 토스트 ──
function showToast(msg) {
  const t = document.getElementById('manageToast');
  document.getElementById('toastMsg').textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 2800);
}
</script>
</body>
</html>
